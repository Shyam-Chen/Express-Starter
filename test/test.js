import { expect } from 'chai';

describe('test', () => {
  it('true is true', () => expect(true).to.equal(true));
});

// import { spy } from 'sinon';

// spy();
// spy(func);
// spy(object, 'method');
